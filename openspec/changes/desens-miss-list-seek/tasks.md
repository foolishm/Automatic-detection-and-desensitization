## 1. 精确跳转

- [x] 1.1 在 `VideoView` 增加公开的 `show_frame(frame_idx)`：主线程暂停、代次加一、立刻更新时间和进度条、清空画面显示「加载中」、清掉显示队列
- [x] 1.2 后台按顺序解码到目标帧，有索引的视频也不使用 `cap.set`；线程不调用 Tk，帧经显示队列交给 `_render_tick`
- [x] 1.3 加载期间忽略旧帧对时间和进度条的改写；代次不匹配的结果丢掉；加载中拖动该侧进度条则这次列表跳转作废，进度条仍走原逻辑

## 2. 漏帧列表

- [x] 2.1 窗口默认改为 1280×720，右侧栏改为 460；列表放在右侧栏底部并占满剩余高度，不放进上方 Canvas
- [x] 2.2 检测成功后填入全部漏脱敏帧，按原视频时间排序；行格式为「时间 原帧号 → 时间 脱敏帧号 原脸数/脱敏脸数」；越界记录不进入；标题在有记录时为「漏脱敏帧（N）」，没有记录时为「漏脱敏帧」并显示「未发现漏脱敏帧」
- [x] 2.3 结果框去掉漏帧逐条文字和「全部人脸帧均已脱敏」
- [x] 2.4 单击一行时对原视频、脱敏视频分别调用 `show_frame`；重新开始检测或重新导入时清空列表并取消两边未完成的跳转

## 3. 验证

- [x] 3.1 测试：假记录算出漏帧后，列表条数、标题和行文本与记录一致，结果框不再出现逐条漏帧；越界帧不在列表中；零漏帧时显示「未发现漏脱敏帧」
- [x] 3.2 测试：单击一行会让两边以列表中的帧号调用精确跳转；再点另一行后，前一次跳转不再改画面
- [x] 3.3 测试：短视频上 `show_frame` 停在指定帧号，画面经过「加载中」；往回跳不使用 `cap.set`
