## 1. 报告数据与文档

- [x] 1.1 在 `_compute_desensitization` 成功路径冻结 `last_desens_report`（两路身份与元数据、对齐、应/已/漏/率/skipped、全量逐帧人脸数、参数快照）；零人脸的 N/A 结果同样冻结
- [x] 1.2 新增 `app/report/desens_docx.py`：用 python-docx 按五节写出 `.docx`，不引用 tkinter；无漏帧时写明未发现；越界数仅大于 0 时出现；接受取消标记，写漏帧表时分批检查，取消则停止并删除本次已写出的临时文件和目标文件
- [x] 1.3 兼容入口再导出拼文档函数；`人脸脱敏率检测工具.spec` 的 `hiddenimports` 加入 `app.report`、`app.report.desens_docx` 和 `docx`

## 2. 按钮与保存

- [x] 2.1 在「开始检测脱敏率」下方增加「生成检测报告」，默认禁用
- [x] 2.2 检测开始、失败、重新导入、`_clear_videos` 时禁用并丢掉冻结结果；仅 `_on_desens_computed` 启用
- [x] 2.3 点击后 `asksaveasfilename` 默认 `原视频主名_vs_脱敏视频主名_脱敏检测报告.docx`；取消对话框不写文件
- [x] 2.4 确认路径后弹出深色「正在保存」并可取消；后台写入临时文件，完成且未取消才替换到目标路径，然后关闭「正在保存」并弹出「保存完成」（含路径）；取消或失败删除本次已生成的临时文件和目标文件，不弹完成

## 3. 验证

- [x] 3.1 测试：未检测时按钮禁用；假记录算出「应脱敏 3 · 已脱敏 2」后按钮可点，导出的结论数字一致，漏帧表含时间与人脸数，且无达标、检测人字样
- [x] 3.2 测试：漏帧为 0 时文档写明未发现漏脱敏帧；取消另存为不产生文件
- [x] 3.3 测试：保存过程中取消后，本次已写出的临时文件和目标报告都被删除，且不出现「保存完成」；成功时在「正在保存」之后出现「保存完成」并带路径
