---
description: 已知 bug 必须写入 .cursor/known-bugs.md，改代码前先对照，避免同一问题再次出现
alwaysApply: true
---

# 已知 Bug 台账

台账文件：`.cursor/known-bugs.md`（唯一记录处）。

## 何时必读

处理缺陷、改 UI/播放/检测逻辑、或准备提交修复前，先读台账全文。命中已有条目时，按「避免再犯」落地，禁止用已记录的错误写法重写同类代码。

## 何时必写

以下情况确认后必须追加或更新条目，禁止只改代码不记账：

- 用户反馈、复现、或自己在本仓库代码里定位到的缺陷
- 修复过程中发现的回归、边界问题
- 同一根因已有条目：只更新状态/修复说明，不另开重复条目

未结合实际代码验证的猜测不要写入。临时调试脚本、一次性环境问题不要写入。

## 写入格式

新条目插在「条目列表」最上方，编号为 `BUG-YYYYMMDD-序号`（当日从 01 递增）。字段必须齐全：

```
## BUG-YYYYMMDD-NN 一句话标题

- 状态: 未修复 | 已修复
- 发现日期: YYYY-MM-DD
- 修复日期: YYYY-MM-DD 或 —
- 相关文件: 路径，多个用逗号分隔
- 现象: 用户可感知的表现
- 根因: 结合代码的真实原因（函数/分支，禁止空泛描述）
- 修复方式: 做了什么、为何这样改
- 避免再犯: 以后改同类代码时禁止/必须怎么做
```

修复完成后把状态改为 `已修复`，补上修复日期、修复方式与避免再犯。未完成验证不得标已修复。
