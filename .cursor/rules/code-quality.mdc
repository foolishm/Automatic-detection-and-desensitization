---
description: 新增修改代码时保持低耦合、高内聚，代码简洁明了
alwaysApply: true
---

# 代码质量：低耦合、高内聚、简洁

新增、修改代码前先看清改动点所属模块的职责，改完后按下面三条自查。

## 低耦合

- 模块只通过公开函数/参数交互，禁止跨模块读写对方的私有属性或内部状态
- 依赖通过参数传入，禁止在函数内部临时 `import` 其他业务模块绕开依赖关系
- UI 层（`app/ui/`）不写检测算法，检测层（`app/detect/`）不引用 tkinter、不弹窗
- 修改一处逻辑若需要同步改多个不相关文件，先考虑是否应抽到共用位置

## 高内聚

- 一个函数只做一件事；出现"并且"才能描述其作用时拆分
- 同一功能的常量、辅助函数放在使用它们的模块内，不散落到无关文件
- 新方法放到职责匹配的 Mixin/模块，禁止因为"顺手"塞进当前正在改的文件

## 简洁明了

- 优先直白写法，不为未出现的需求预留抽象层、开关和参数
- 分支条数、嵌套层级明显变多时先提炼辅助函数，再继续加逻辑
- 删除无用变量、注释掉的代码、重复的判断；同一逻辑禁止复制粘贴第二份
- 命名说明用途，能从名字看懂就不再加解释性注释

```python
# ❌ 不好：函数同时做读取、判断、弹窗，且直接读别的模块内部状态
def check_and_warn(app):
    if app._view._frames > cfg.MAX and app._view._dirty:
        messagebox.showwarning("提示", "帧数过多")

# ✅ 好：判断和提示分离，依赖通过参数传入
def frames_exceeded(frame_count, limit):
    return frame_count > limit

if frames_exceeded(view.frame_count(), cfg.MAX):
    messagebox.showwarning("提示", "帧数过多")
```
