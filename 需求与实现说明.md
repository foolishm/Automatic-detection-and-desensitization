# 人脸脱敏率检测工具 — 需求与实现说明

> 项目目录：`D:\tool\workbuddy\face_video_detector\`
> 主程序：`face_video_detector.py`
> 核心算法：`desensitization_checker.py`
> 配置文件：`config.json`（首次修改参数时自动生成）
> 启动方式：双击 `启动工具.bat`

---

## 一、项目概述

本工具用于**验证视频人脸脱敏（打码）的完整性与成功率**。核心场景：给定「原视频」和「脱敏后视频」（两者为同一时间段内容，但帧率可能不同），计算脱敏率——即原视频中「应脱敏的人脸帧」有多少在脱敏视频中真正被处理。

---

## 二、界面结构

### 1. 顶栏
- 标题「人脸脱敏率检测」+ 检测器徽标（YuNet）
- 右侧功能按钮：**「检测」 / 「参数设置」**（页面切换）、**「肤色过滤：开/关」**

### 2. 页面切换
主界面分两个页面，顶栏按钮切换，当前页高亮（蓝色）：

| 页面 | 内容 |
|------|------|
| **检测页**（默认） | 双视频窗口 + 右侧脱敏检测面板 |
| **参数设置页** | 检测参数列表 + 保存 / 恢复默认值按钮 |

### 3. 检测页布局
```
┌──────────────────────────────────────────────────────────┐
│  人脸脱敏率检测   YuNet     [检测][参数设置]  [肤色过滤:开] │
├──────────────────────────┬───────────────────────────────┤
│ [原视频窗口]             │ [脱敏视频窗口]                 │
│  ▢显示检测框 [播放][导入] │ ▢显示检测框 [播放][导入]       │
│  ┌────────────────┐     │ ┌────────────────┐             │
│  │    视频画面     │     │ │    视频画面     │             │
│  └────────────────┘     │ └────────────────┘             │
│  ──播放进度条── 时间      │ ──播放进度条── 时间             │
│  文件/分辨率/帧率/总帧数  │  文件/分辨率/帧率/总帧数        │
│  分析中… X/Y 帧 (百分比) │  分析中… X/Y 帧 (百分比)        │
│  ▶ 检出人脸帧 常显       │  ▶ 检出人脸帧 常显             │
│  ──分析进度条──          │  ──分析进度条──                │
├──────────────────────────┴───────────────────────────────┤
│                            │  脱敏率检测                  │
│                            │  原视频 分析中X/Y帧          │
│                            │  脱敏视频 分析中X/Y帧        │
│                            │  [开始检测脱敏率]            │
│                            │  检测进度 X/Y帧              │
│                            │  公式(常显)                 │
│                            │  检测结果(滚动)              │
└──────────────────────────────────────────────────────────┘
```

---

## 三、功能需求清单

### 1. 视频导入
- 「原视频」「脱敏视频」两个独立导入入口（各自播放窗口标题栏里的「导入视频」按钮）。
- 支持格式：mp4 / avi / mov / mkv / flv / wmv / webm / m4v。
- 导入后立即显示第一帧，并**后台自动开始逐帧人脸检测**。

### 2. 双窗口视频预览
- 左右两个独立播放窗口，各有：**播放/暂停**、**进度条拖动**、时间戳。
- 播放进度条紧贴视频画布下方。

### 3. 显示检测框开关（每窗口独立）
- 每个窗口标题栏有「▢ 显示检测框」勾选框。
- 勾选后播放时在画面上叠加人脸框（绿色框 + Face 序号）。
- **两个窗口独立开关，各自持久化**；实时生效（无需重新导入）。
- 检测框数据来自预处理结果，需等分析完成后才有框显示。

### 4. 人脸检测（预处理）
- 导入后后台自动逐帧检测（YuNet 深度学习检测器）。
- 记录：「有人脸的帧号 + 人脸框坐标 + 帧率 + 总帧数」供脱敏率计算。
- 进度实时显示「已分析 X/Y 帧 (百分比)」。

### 5. 检测参数（可调，见「参数设置页」）
| 参数 | 默认值 | 范围 | 说明 |
|------|--------|------|------|
| 检测置信度阈值 | 0.6 | 0.1~0.99 | 越高越严格（挡耳朵/半边头/侧脸）|
| 最小人脸边长(px) | 20 | 5~200 | 过滤小于该边长的框 |
| 肤色过滤 | 开 | 开/关 | 挡轮胎等肤色≈0 的误检 |
| 肤色校验最小边长(px) | 60 | 20~300 | ≥此边长才做肤色校验 |
| 肤色占比阈值 | 0.10 | 0.01~0.5 | 低于此值判非人脸 |
| 轨迹确认帧数 | 3 | 1~20 | 连续几帧确认 |
| 轨迹丢失容忍帧数 | 5 | 1~30 | 丢失几帧移除 |
| **五官二次校验** | 关 | 开/关 | 挡「只有耳朵/后脑勺」误检（见下方「开启条件」）|
| 五官关键点最小数量 | 3 | 1~10 | 框内需定位到的眼/鼻/嘴关键点下限 |

### 6. 五官二次校验（重要，默认关闭）
- **用途**：在 YuNet 检出人脸框后，用 MediaPipe FaceLandmarker 二次检测框内是否有「眼睛/鼻子/嘴巴」关键点，从而挡掉「只有耳朵/后脑勺（完全背对镜头）」的误检——这类画面只看得到后脑勺、看不到任何五官，本不构成"需要脱敏的人脸"。
- **判定规则**：框内眼/鼻/嘴关键点数量 ≥「五官关键点最小数量」（默认 3）才保留。
- **开启条件（务必阅读）**：MediaPipe 只能检测**较大且清晰**的人脸（边长约 **100px 以上**）。因此：
  - ✅ 人脸较大、清晰、正对或侧对镜头时，开启有效。
  - ❌ **远距离监控（人脸仅几十像素）时切勿开启**——MediaPipe 检不出小人脸的五官，会误杀**全部**真脸（实测：小人脸场景开启后检出归零）。
- 该开关默认关闭，需在参数设置页自行开启并保存。

### 7. 参数设置页
- 顶栏「参数设置」按钮进入，内嵌页面（非弹窗），参数列表可滚动（滚轮+滚动条），「保存/恢复默认值」按钮固定底部。
- 每个参数：中文标签 + 说明 + 输入控件（数值用 Spinbox，布尔用**「开/关」切换按钮**，绿色=开、灰色=关）。
- **「保存」**：校验 → 应用 → 持久化到 `config.json` → **清空已导入的视频数据**（旧参数分析结果已失效，需重新导入视频分析）。
- **「恢复默认值」**：把控件填回默认值（点保存后生效）。
- **永久记忆**：下次启动自动从 `config.json` 恢复。

### 8. 开关持久化
以下开关状态均持久化，重启后恢复：
- 原视频「显示检测框」开关（`show_boxes_src`）
- 脱敏视频「显示检测框」开关（`show_boxes_dst`）
- 「肤色过滤」开关（纳入参数体系）
- 「五官二次校验」开关（纳入参数体系）

### 9. 脱敏率检测
- 右侧「开始检测脱敏率」按钮。
- **数据未分析完成时**提示「数据还未收集完成，无法进行检测」。
- 检测过程中显示「检测进度 X/Y 帧」进度条。

### 10. 脱敏率计算逻辑
- **时间对齐**：帧率可不同，按时间戳对齐 `时间=帧号/fps`、`对应帧=round(时间×另一fps)`。任意帧率组合（含脱敏视频帧率≥原视频）都能正确处理。
- **边界保护**：对齐越界的帧跳过、不计入分母。
- **公式**：`脱敏率 = 已脱敏人脸数 ÷ 帧率换算后原视频应脱敏人脸数 × 100%`。换算公式：`脱敏帧号 = round(原帧号 ÷ 原帧率 × 脱敏帧率) + 帧偏移`。报告分列原视频人脸数、脱敏视频人脸数、帧率换算后原视频应脱敏人脸数、帧率换算后脱敏视频检测出人脸数；帧数用对应四项：原视频人脸帧数、脱敏视频人脸帧数、帧率换算后原视频应脱敏帧数、帧率换算后脱敏视频检测出帧数。已脱敏人脸数 = 各次对齐的 max(0, 该帧原视频人脸数 − 该帧脱敏视频人脸数) 之和。
- **边界情况**：原视频 0 人脸帧 → 显示「无需脱敏 / N/A」。

### 11. 结果展示（右侧面板）
- 原视频 / 脱敏视频状态 + 各自「分析中/完成 X/Y 帧」帧数进度。
- 检测进度条（X/Y 帧）。
- 常显的「脱敏率计算公式」（含帧率不同时的对齐说明）。
- 检测结果框：帧率、帧数、脱敏率、漏脱敏帧列表。

---

## 四、误检抑制能力

| 手段 | 机制 |
|------|------|
| 置信度阈值（可调） | YuNet 置信度低于阈值不检出 |
| 最小人脸边长（可调） | 过滤极小框（阴影/噪点/远距离小人脸）|
| 肤色过滤（可调） | HSV 肤色范围，挡轮胎/配件等肤色≈0 误检 |
| 五官二次校验（可调，默认关） | MediaPipe 检测框内眼/鼻/嘴，挡「只有耳朵/后脑勺」误检；仅大脸场景有效 |
| 轨迹稳定性过滤 | 连续几帧出现才确认，过滤"单帧闪烁"误检 |

**已知限制**：远距离监控场景下，"半只耳朵/半边头"与极小人脸（10~40px）在像素层面难以可靠区分。YuNet 的置信度、内置关键点几何对真假脸无区分度，MediaPipe 检不出此尺寸小人脸。**实际可用手段是调大「最小人脸边长」**来挡掉小框误检（代价是过滤远距离小人脸）。

---

## 五、配置文件 config.json

存储在项目目录，保存所有可调参数与开关状态。结构示例：

```json
{
  "YUNET_SCORE_THRESHOLD": 0.6,
  "MIN_FACE_SIZE": 20,
  "SKIN_FILTER_ENABLED": true,
  "SKIN_MIN_SIZE": 60,
  "SKIN_RATIO_THRESHOLD": 0.1,
  "CONFIRM_FRAMES": 3,
  "LOST_FRAMES": 5,
  "show_boxes_src": false,
  "show_boxes_dst": false
}
```

首次运行程序不会有该文件，首次修改参数或开关后自动创建。

---

## 六、关键技术实现

### 1. 线程安全
- 所有 GUI 更新通过「子线程写共享变量/队列 → 主线程 `after` 轮询消费」模式，避免 tkinter 多线程崩溃。
- 重新导入视频时，通过「代次标记 generation」让旧分析线程立即退出，避免新旧线程数据冲突。

### 2. 进度条自噬防护
- `ttk.Scale` 的 `.set()` 会触发 command 回调，程序内部更新进度时必须加 `_updating_progress` 标志区分"程序更新"与"用户拖动"，否则会导致播放卡死（本项目踩过此坑两次）。

### 3. Canvas 滚动区宽度
- Canvas+Scrollbar 滚动内容须绑定 `<Configure>` 动态同步 window 宽度，否则内容只显示左边一部分。

---

## 七、已知边界与约定

1. **时长相同前提**：两视频须为同一时间段内容（时长一致），对齐按时间戳进行。
2. **帧率任意**：不限于 25/15，从视频动态读取各自的 fps，脱敏视频帧率≥原视频也兼容。
3. **脱敏方式**：当前判定针对「马赛克打码 + 检不出人脸」；若用「涂黑/覆盖」或「模糊」，判定条件需调整。
4. **0 人脸帧**：原视频未检出人脸时，脱敏率显示「无需脱敏 / N/A」。

---

## 八、待优化项

- [ ] 清理文件中遗留的旧版单视频播放死代码（不影响运行，可精简）。
- [ ] 预处理进度可加采样加速选项（当前逐帧检测，长视频较慢）。
- [ ] 支持「模糊」「涂黑」等其他脱敏方式的判定。
- [ ] 「半只耳朵」误检的进一步抑制（结合肤色+亮度+尺寸的启发式，或引入更适合远距离小人脸的检测器）。
