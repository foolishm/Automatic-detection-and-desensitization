# 代码架构

本文是本仓库的架构约定。**新增、修改、修复都必须按本文落地**，禁止把实现重新堆进 `face_video_detector.py`。Cursor 规则 `.cursor/rules/code-architecture.mdc` 指向本文。

## 分层总览

```
启动 / 测试 / 打包
        │
        ▼
face_video_detector.py     兼容入口：只再导出公开名字，不含业务实现
        │
        ├── app/main.py            Tk 启动
        ├── app/ui/app.py          FaceVideoApp 组装 Mixin + 检测页布局
        ├── app/detect/            人脸检测与轨迹
        ├── app/video/             视频格式、计帧、跳转
        ├── app/settings.py        检测参数与 config.json
        └── desensitization_checker.py   脱敏率算法（从兼容入口拿检测器）
```

主窗口类：

```python
class FaceVideoApp(TitlebarMixin, ParamsMixin, DesensMixin, PipelineMixin)
```

UI 新方法放到对应 Mixin，由 `FaceVideoApp` 继承；不要在 shim 里写类或逻辑。

## 目录职责

| 路径 | 职责 |
| --- | --- |
| `face_video_detector.py` | 兼容入口。`启动工具.bat`、测试、`desensitization_checker.py`、PyInstaller spec 都从这里进 |
| `app/main.py` | `main()`：建 `Tk`、挂 `FaceVideoApp`、进主循环 |
| `app/paths.py` | `_resource_path`（模型，打包走 `_MEIPASS`）、`_app_dir`（可写配置，打包走 exe 旁） |
| `app/settings.py` | 检测参数、`PARAM_DEFS`、`load_params` / `apply_params` / `config.json` |
| `app/theme.py` | 配色与标题栏图标常量 |
| `app/video/formats.py` | `VIDEO_EXTS`、裸 H.264 计帧、`decode_seek`、文件对话框过滤器 |
| `app/detect/detector.py` | `FaceDetector`、`skin_ratio` |
| `app/detect/mosaic.py` | 马赛克定位与涂抹预处理：`checker_mosaic_masks`（2~3px 全分辨率，4px 及以上粗到精；细格命中则跳过粗格）、`mask_mosaic`、`mask_rects`（拆分 / 合并）；预处理记录用 `frame_mosaic_record`（棋盘格 / 实心矩形）、`rect_coverage` |
| `app/detect/analyze.py` | 单帧分析 `analyze_frame`（人脸 + 马赛克）与多线程有序流水线 `FrameAnalyzerPool`，预处理线程只负责解码与收结果 |
| `app/detect/track.py` | `FaceTracker`、`TrackGate` |
| `app/report/desens_docx.py` | 把冻结的脱敏率结果写成 Word 报告（`write_desens_docx`）；不引用 tkinter，取消时删除本次已写出的文件 |
| `app/ui/app.py` | 主窗口组装、检测页左右布局、右侧脱敏面板 |
| `app/ui/titlebar.py` | 自绘标题栏、托盘、最大化到工作区 |
| `app/ui/video_view.py` | 原视频 / 脱敏视频预览播放（`VideoView`，兼容名 `_VideoView`） |
| `app/ui/drop.py` | 拖放路径解析、`pick_dropped_video` |
| `app/ui/params.py` | 参数设置页 |
| `app/ui/dialogs.py` | 深色弹窗模板：`showinfo` / `showerror` / `showwarning`，以及可取消的「正在保存」 |
| `app/ui/desens.py` | 双路导入、预处理（人脸框；马赛克跟随 `MOSAIC_MASK_ENABLED`）、脱敏率检测 UI（按对齐后的人脸数算成功率，帧数另列，不重读视频） |
| `app/ui/pipeline.py` | 旧单路检测流水线（仍由 Mixin 挂在主窗口上） |
| `desensitization_checker.py` | 脱敏率计算（命令行或被 UI 调用） |
| `tests/` | 全部测试脚本 |
| `config.json` | 运行时配置（工程根 / exe 旁） |
| `人脸脱敏率检测工具.spec` | PyInstaller 单文件打包 |

没有对应文件时，在**同一层**新建模块（例如新弹窗能力进 `app/ui/dialogs.py`，新视频格式进 `app/video/`）。不要塞进 shim 或无关文件。新增 `app.*` 子模块必须同步写进 spec 的 `hiddenimports`。

## 依赖方向

允许：

- `app/ui/*` → `app/detect`、`app/video`、`app/settings`、`app/theme`、`app/ui/dialogs`、`app/report`
- `app/report/*` → `app/settings`、`python-docx`（不引用 tkinter）
- `desensitization_checker.py` → `face_video_detector`（`FaceDetector`、配置名、`skin_ratio`）
- `tests/*` → `face_video_detector as f`

禁止：

- 在 `face_video_detector.py` 里写窗口、检测、播放逻辑
- UI 模块互相抄一份主题色或再造一套弹窗
- 测试 `import app.ui.xxx` 绕过兼容入口

## 运行时配置

会随参数页 / 肤色按钮变化的值，必须读模块上的当前属性：

```python
from app import settings as cfg
skin_on = cfg.SKIN_FILTER_ENABLED
```

禁止：

```python
from app.settings import SKIN_FILTER_ENABLED
```

后者是 import 时快照，`apply_params` 改完之后检测器看不到新值。`FaceDetector` / `TrackGate` 内部同样走 `cfg.NAME`。

## 弹窗与文件对话框

- 提示 / 错误 / 警告：`from app.ui import dialogs as messagebox`，调用 `showinfo` / `showerror` / `showwarning`。有主窗口时传 `parent=self.root`。
- 禁止 `from tkinter import messagebox`。
- 选文件仍用 `from tkinter import filedialog`。
- 兼容入口把 `app.ui.dialogs` 再导出为 `messagebox`，测试替换 `f.messagebox.showinfo` 即可。

新弹窗种类（例如确认框）加在 `app/ui/dialogs.py`，沿用同一套深色顶栏和配色，不要另起原生对话框。

## 测试

- 只放 `tests/test_*.py`，禁止在工程根再放 `test_*.py`。
- 开头：

```python
import _bootstrap
import face_video_detector as f
```

- `_bootstrap.py` 把工程根加入 `sys.path`，以便 `python tests/xxx.py` 能 import 入口。
- 替换弹窗 / 选文件：`f.messagebox` / `f.filedialog`。

## 启动与打包

- 开发启动：`启动工具.bat` → `venv\Scripts\pythonw.exe face_video_detector.py`
- 打包：`.\venv\Scripts\pyinstaller.exe --noconfirm 人脸脱敏率检测工具.spec`
- 产物：`dist/人脸脱敏率检测工具.exe`

## 代码风格（本仓库）

- 新代码 Allman 大括号风格（Python 为 `if` / `def` 的下一行缩进块）。
- 函数单一 `return`（先把结果放到变量里，最后返回）。
- 分支处加注释，说明该分支做什么。
- 缺陷记入 `.cursor/known-bugs.md`，改同类代码前先对照台账。
